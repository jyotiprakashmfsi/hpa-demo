apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mern-hello-world
  template:
    metadata:
      labels:
        app: mern-hello-world
    spec:
      containers:
        - name: hello-world
          image: jyotiprakashh/hello-world
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"